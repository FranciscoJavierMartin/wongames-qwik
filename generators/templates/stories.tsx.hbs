import type { Meta, StoryObj } from 'storybook-framework-qwik';
import {{pascalCase name}}, { type {{pascalCase name}}Props } from '.';

const meta: Meta = {
  title: '{{pascalCase name}}',
  component: {{pascalCase name}},
  parameters: {
    layout: 'fullscreen',
  },
};

export default meta;

export const Default: StoryObj<{{pascalCase name}}Props> = {
  render: (props) => <{{pascalCase name}} {...props} />,
};
